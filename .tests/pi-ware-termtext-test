#load functions and variables
source "$HOME/pi-ware/func/api" || echo "failed to source $HOME/pi-ware/func/api"

#Main
term-text
term-text "uname -m && sleep 3"
term-text "uname -m && sleep 3" "Aarch Type"
